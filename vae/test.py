# import numpy as np
# a = np.ones((90*30, 30))
# print(a.shape)
# a = a.reshape(-1, 30, 30)
#
# a = np.mean(a)
# print(a)
# b = np.mean(a, axis=0)
# print(b.shape)
# c = np.mean(a, axis=1)
# print(c.shape)
# d = np.mean(a, axis=2)
# print(d.shape)
